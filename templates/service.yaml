apiVersion: v1
kind: Service
metadata:
  name: {{ include "keycloak-helm.fullname" . }}
  labels:
    {{- include "keycloak-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "keycloak-helm.selectorLabels" . | nindent 4 }}
